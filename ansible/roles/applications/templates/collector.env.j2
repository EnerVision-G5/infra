# Déployé par le rôle Ansible "applications" — ne pas éditer sur l'hôte.
# Fichier hors Git (cf. .gitignore).
#
# Le collector ne lit aujourd'hui AUCUNE variable d'environnement : bootstrap
# Kafka, URL de l'API mock, topic, sites et intervalle sont écrits en dur dans
# son code. Ce fichier est donc vide. Quand le service deviendra configurable,
# ajouter ici les ${VARIABLE} qu'il lira (KAFKA_BOOTSTRAP, MOCK_API_URL, …).
