# Déployé par le rôle Ansible "applications" — ne pas éditer sur l'hôte.
# Le front est une image statique : sa configuration (VITE_API_BASE_URL) est
# figée au build par le pipeline. Ce fichier est conservé pour l'homogénéité
# des stacks (un compose.yml + un .env par application) et reste vide de secret.
