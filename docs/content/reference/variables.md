# Variables (vars.yml)

Tableau exhaustif des variables non sensibles du groupe all.

!!! note "Ébauche — phase 1"
    Cette page sera rédigée dans une phase ultérieure. Plan prévu :

- Nom · rôle consommateur · valeur · description
- Idéalement extrait automatiquement du fichier (snippets) pour éviter la dérive
