# Domaines & routage

La correspondance domaine public → routeur Traefik → conteneur → port.

!!! note "Ébauche — phase 1"
    Cette page sera rédigée dans une phase ultérieure. Plan prévu :

- Table domaine → service → routeur → port interne
- app. / api. / s3. / web. / garage. / grafana. / traefik.
- État TLS (terminaison, certresolver, lignes commentées) — détaillé dans Exploitation › DNS & TLS
