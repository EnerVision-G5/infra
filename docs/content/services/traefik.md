# Traefik

Point d'entrée unique : routage HTTP, terminaison TLS, découverte des conteneurs par labels.

!!! note "Ébauche — phase 1"
    Cette page sera rédigée dans une phase ultérieure. Plan prévu :

- Image, réseau proxy_network, entrypoints, dashboard
- Comment un service se déclare (labels traefik.* dans son compose)
- Variables (traefik.env), certresolver
- Dépannage : 404, service non routé, réseau
