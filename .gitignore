# Secrets et variables d'environnement (jamais dans Git, cf. décision V2)
*.env
.env
.env.*
!.env.example
vault_pass*
.vault_pass*
*.vault
secrets.yml

# Ansible
*.retry
ansible/collections/
ansible/inventories/**/host_vars/*_secrets.yml

# Clés
*.pem
*.key
id_rsa*
id_ed25519*

# Documentation (MkDocs) — build local, jamais versionné
docs/site/
docs/.cache/

# Divers
.DS_Store
*.log

# Terraform : état et plans (contiennent la description réelle des ressources,
# l'état vit sur Azure), cache des fournisseurs.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
